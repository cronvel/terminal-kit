
OK	Terminal.js file and object separated from termkit.js
	Terminal#object2attr
OK	Move ScreenBuffer#color2index to termkit.color2index (through misc.js)
	Terminal#put() that mimic ScreenBuffer#put()
	Create a Widget object, that is recursive: it can have Widget children that can cycle focus using TAB and SHIFT_TAB
	Button should inherit from Widget
	Move inputField() area mode to an InputText object that inherit from Widget
	Turn Forms into a Widget of Widgets
	Layout should be a Widget, and each leaf boxes' ScreenBuffer should be Widgets too
