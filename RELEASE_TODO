
Must have:

	ScreenBuffer full-width chars bugfixes
	TextBuffer full-width chars bugfixes
	Extract ScreenBuffer loader/saver, one file per version



Nice to have:

	Input processing refacto (take it from Atomic Terminal)
	Document the Document feature (Button, Element, TextInput, Text, Layout, etc)
	24 bits ScreenBuffer
	
